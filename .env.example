# Copy to .env and fill in. Do not commit .env (secrets).

# --- Supabase Contact Form (Option 2) ---
# Get from Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# --- Events API ---
GOOGLE_CALENDAR_ICAL_URL=

# --- Contact form email ---
# Address that receives contact form submissions (required for /api/contact)
CONTACT_EMAIL=

# Option A: Gmail SMTP (use App Password with 2FA enabled)
SMTP_USER=
SMTP_PASS=

# Option B: Resend (https://resend.com) – use API key as SMTP user, password leave empty; set SMTP_HOST=smtp.resend.com, SMTP_PORT=465
# RESEND_API_KEY=
# SMTP_HOST=smtp.resend.com
# SMTP_PORT=465

# Optional: override SMTP host/port (defaults: Gmail smtp.gmail.com:465)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465

# Optional: "From" address (defaults to SMTP_USER for Gmail; set for Resend e.g. onboarding@resend.dev or your verified domain)
# SMTP_FROM=
